#define PEIO_GUI_EXPORTING
#include "Formats.h"

namespace Peio::GUI {

	DXGI_FORMAT Formats::ToDXGIFormat(AVPixelFormat format)
	{
		switch (format) {
			
		}
	}
	AVPixelFormat Formats::ToAVPixelFormat(DXGI_FORMAT format)
	{
		switch (format) {

		}
	}

}